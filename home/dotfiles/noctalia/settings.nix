{
  programs.noctalia = {
    enable = true;

    settings = {
      wallpaper.enabled = false;

      theme = {
        mode = "dark";
        source = "custom";
        custom_palette = "raiden-theme"; 
      };
    };
  };
}